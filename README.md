# image_analyzer
Python Image Analyzer for astronomical FITS Images

This Python script is an interactive tool for analyzing astronomical FITS images. It displays the image, lets users select stars, performs 2D Gaussian fitting to estimate centroid and FWHM, and computes aperture photometry with S/N. Results are visualized, saved as plots, and logged to a file.
